files:
  "/etc/cron.d/actssl.sh":
    mode: "000644"
    owner: root
    group: root
    content: |
      1 4 1,16 * * root aws s3 cp s3://conexpar-us-east-1-bounce/configssl/letsencrypt/ /etc/letsencrypt/ --recursive 

commands:
  remove_old_cron:
    command: "rm -f /etc/cron.d/*.bak"
